/**
 * @api {get} /area/:id Get information about an AREA
 * @apiParam {Uint} id Area id to get information from
 * @apiName AreaInfo
 * @apiGroup Area
 *
 * @apiHeader {String} Authorization="Bearer (token)" Token of the user logged in.
 * // TODO here add all the future elements of an area (the actions and reactions it contains)
 */

 /**
 * @api {post} /area Create a new AREA
 * @apiName AreaCreate
 * @apiGroup Area
 *
 * @apiHeader {String} Authorization="Bearer (token)" Token of the user logged in.
 * // TODO here add all the future elements of an area (the actions and reactions it contains)
 */

 /**
 * @api {put} /area/:id Update information about an AREA
 * @apiParam {Uint} id Area id to get information from
 * @apiName AreaUpdate
 * @apiGroup Area
 *
 * @apiHeader {String} Authorization="Bearer (token)" Token of the user logged in.
 * // TODO here add all the future elements of an area (the actions and reactions it contains)
 */

 /**
 * @api {delete} /area/:id Delete an AREA
 * @apiParam {Uint} id Area id to get information from
 * @apiName AreaInfo
 * @apiGroup Area
 *
 * @apiHeader {String} Authorization="Bearer (token)" Token of the user logged in.
 * // TODO only put error code and success code
 */

 // TODO AJOUTER TRIGGER HISTORY ICI
